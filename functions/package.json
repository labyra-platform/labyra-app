{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "pnpm build && firebase emulators:start --only functions",
    "shell": "pnpm build && firebase functions:shell",
    "start": "pnpm shell",
    "deploy": "firebase deploy --only functions",
    "deploy:single": "firebase deploy --only functions:NAME",
    "logs": "firebase functions:log",
    "logs:tail": "firebase functions:log --only NAME"
  },
  "engines": {
    "node": "24"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.9.0",
    "firebase-functions": "^7.0.0",
    "@anthropic-ai/sdk": "^0.95.1",
    "google-auth-library": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "firebase-functions-test": "^3.4.1"
  },
  "private": true,
  "// R171-0a": "Aligned TS version with root (^5.7.2). Region asia-southeast1 set in code (setGlobalOptions)."
}
