# Design rules §12 — ratchet baseline. See scripts/check-design-tokens.sh.
#
# These are debts, not budgets. Each number is how many violations existed when
# the guard went in; the guard only lets them fall. Lower the number in the same
# commit that fixes them — that is the whole mechanism.
#
# Not yet checked, and why:
#   spacing        — 3235 classes in src/, and the seven §1 values don't cover
#                    gap-1 / gap-1.5 / px-2 / py-1. Needs migrating before it
#                    can be locked, or the lock just unstyles the app.
#   shadcn Card    — 2 files still import ui/card (gap-6 inside py-6 = 24:24,
#                    plus border AND shadow, §1+§4). Migrating to Panel was a
#                    per-feature job, not a grep: R512 settings, R513 deviation,
#                    R514 spectra+computation, R515 superadmin+papers+materials.
#                    lab-context-form and ai-preferences-form deliberately stay
#                    — their page already renders the heading, so a Panel title
#                    would duplicate it. That is a design call, not a migration.
#   aria-labelledby — NOT a countable metric, and the number that used to sit
#   here was wrong. Grepping the string counts only files that hand-roll the
#   attribute; the 25 files that render <Panel> get the landmark from the
#   primitive and never contain the string. Landmark coverage is ~25 Panel
#   surfaces + 5 hand-rolled, and is enforced by using Panel, not by a grep.

arbitrary_text_size=100
off_scale_size=32
off_scale_weight=124
hardcoded_color=4
